// Phase 1A.1 scaffold placeholder only.
//
// The real Tourist Web Map (photo markers, categories, side panel, search,
// voice guide, QR) is out of scope until Phase 1E onward per
// docs/stages/STAGE_1_BLUEPRINT.md. This app exists in Phase 1A purely as a
// foundation so the workspace/toolchain is validated against two apps from
// the start.
export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '3rem' }}>
      <h1>Tourist Map System</h1>
      <p>Coming soon.</p>
    </main>
  );
}
